# 自動デプロイ設定開始
# main ブランチへの push で https://sport-performance-library.web.app に自動デプロイ
name: Deploy to Firebase Hosting

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: app/package-lock.json

      - name: Install app dependencies
        run: cd app && npm ci

      - name: Build
        run: cd app && npm run build

      - name: Check Firebase token
        run: |
          if [ -z "${{ secrets.FIREBASE_TOKEN }}" ]; then
            echo "::error::FIREBASE_TOKEN が GitHub Secrets に設定されていません。firebase login:ci でトークンを取得し、Settings > Secrets に登録してください。"
            exit 1
          fi

      - name: Deploy to Firebase Hosting
        uses: w9jds/firebase-action@v13.4.1
        with:
          args: deploy --only hosting --project sport-performance-library
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
